# 🚀 SecureNet Landing Page

A fully responsive, high-converting landing page template designed for a fictional VPN service ("SecureNet"). This project demonstrates a foundational understanding of web development, focusing on layout structure, CSS Flexbox, and modern UI design principles.

## 🌟 Key Features

* **Responsive Layout:** Fully fluid design that adapts to mobile, tablet, and desktop screens.
* **Flexbox Grid System:** Uses CSS Flexbox to manage columns, "Zig-Zag" alternating sections, and content alignment.
* **Modern UI/UX:** Features a soft color palette (Cream/Dark Green), pill-shaped buttons, drop shadows, and hover animations.
* **Semantic HTML:** Built with accessibility in mind using proper tags (`<header>`, `<main>`, `<section>`, `<nav>`).
* **Interactive Elements:** Includes hover states on buttons and feature cards to improve user engagement.

## 🛠️ Technologies Used

* **HTML5:** Structure and semantic markup.
* **CSS3:** Styling, variables (`:root`), Flexbox layouts, and media queries.
* **Assets:** Custom generated illustrations for feature sections.

## 📂 Project Structure

```text
├── index.html        # The main HTML structure
├── style.css         # All CSS styling and variables
├── images/           # Folder containing project assets
│   ├── secure-access.png
│   ├── safe-internet.png
│   └── device-mockup.png
└── README.md         # Project documentation
